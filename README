usage: 750words.py [-h] {stats,edit,cat,path,config,wc} ...

positional arguments:
  {stats,edit,cat,path,config,wc}
    cat                 cat the text
    config              modify or view configuration
    edit                edit the text
    path                get the path to the text file
    stats               view stats
    wc                  view word count

optional arguments:
  -h, --help            show this help message and exit
