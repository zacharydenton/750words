usage: 750words.py [-h] {wc,edit,cat,path,analyze,config} ...

positional arguments:
  {wc,edit,cat,path,analyze,config}
    analyze             view analysis
    cat                 cat the text
    config              modify or view configuration
    edit                edit the text
    path                get the path to the text file
    wc                  view word count

optional arguments:
  -h, --help            show this help message and exit
